fastapi==0.116.1
# Uvicorn 0.35 requires Python >=3.9; Ubuntu 20.04 ships Python 3.8.
# 0.33.0 keeps the same ASGI behavior and remains compatible with Python 3.8.
uvicorn[standard]==0.33.0
python-multipart==0.0.20
httpx==0.28.1
# Allows Pydantic to evaluate PEP 604 annotations on Python 3.8.
eval-type-backport==0.2.2
